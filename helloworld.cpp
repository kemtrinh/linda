#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int a, b;
        cin >> a >> b;
        int s;
        while(a--)
        {
            int d;
            cin >> d;
            if(d == b) s++;
        }
        cout << (d!=0) ? << d << endl : << ""
    }
    return 0;
}